# install django and pip by following commands
  pip install django
  python -m pip install --upgrade pip

# setup the virtual env
   1. py -m venv env
   2. cd env/Scripts
   3. . activate
   
# then run python manage.py runserver
